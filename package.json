{
  "name": "bsc-plugin-auto-google-fonts",
  "version": "1.0.2",
  "description": "BrighterScript plugin to automatically download and use Google Fonts in Roku SceneGraph components",
  "main": "dist/google-fonts-plugin.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky install"
  },
  "keywords": [
    "brighterscript",
    "roku",
    "google-fonts",
    "plugin",
    "ropm"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^12.20.55",
    "brighterscript": "^0.69.4",
    "husky": "^9.1.7",
    "ts-node": "^10.9.2",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "css-font-face-src": "^1.0.0",
    "node-fetch": "^2.7.0"
  }
}
